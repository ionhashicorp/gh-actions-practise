name: lint test deploy
on: [push]
jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - name: lint
        run: npm run lint
      - name: dependency
        run: npm ci
      - name: test
        run: npm run test
      - name: deploy
        run: npm run build
