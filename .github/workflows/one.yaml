name: lint test deploy
on: [push]
jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - name: lint
        run: npm run lint
      - name: dependency
        run: npm ci
      - name: deploy
        run: npm run build
